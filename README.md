# Courier-Bridge
Courier-Bridge is a Java-based parcel tracking and courier integration system that manages shipments, generates tracking numbers, records delivery status history, and standardises updates from different courier providers through a single REST API.
